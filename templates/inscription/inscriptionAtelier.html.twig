{% extends 'base.html.twig' %}
{% block body %}
    {{form_start(form)}}
        <div class="form-group  align-middle container">
            <div class="row">
                <div>
                    <h5>Choix des ateliers (5 ateliers maximum - a cocher)</h5>                    
                    {{ form_widget(form.ateliers) }}
                </div>
                <div>
                    <h5>Bulletin Réservation hôtel</h5>
                    {{ form_widget(form.hotels) }}
                </div>
            </div>
        </div>
    {{form_end(form)}}
{% endblock %}