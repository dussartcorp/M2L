{% extends 'base.html.twig' %}
{% block body %}

	{% if app.user %}
		{% if is_granted('ROLE_USER') or is_granted('ROLE_SUPER') or is_granted('ROLE_ADMIN')%}
			<div>
			{{form_start(nuites)}}
				<div class="InscriptionBox container">
					<div>
						<div class="form-group">
							<div class="form-check" row>
								<h5>Choix des ateliers (5 ateliers maximum - a cocher)</h5>
								{{ form_row(nuites.inscription.ateliers) }}
							</div>
						</div>
						<hr class="my-4">
						<div class="row">
							<h5 id="libelleResa" style="display:block">Réservation hôtel</h5>
							 <div id="dropHotel" class="dropdown">
								<button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1"  data-toggle="dropdown" aria-expanded="false">Ajouter</button>
								<ul class="dropdown-menu">
									<li id="liAjoutHotel">
										<a class="dropdown-item" onclick="ajouterHotel()">Ajouter Hôtel</a>
									</li>
									<li id="liAjoutResto">
										<a class="dropdown-item" onclick="ajouterRestauration()">Ajouter restauration</a>
									</li>
								</ul>
							</div>
							<div class="dropdown">
								<button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuButton1" data-toggle="dropdown" aria-expanded="false">Supprimer</button>
								<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
									<li id="liSuppHotel">
										<a class="dropdown-item" onclick="supprimerHotel()">Supprimer hôtel</a>
									</li>
									<li id="liSuppResto">
										<a class="dropdown-item" onclick="supprimerRestauration()">Supprimer restauration</a>
									</li>
								</ul>
							</div> 
						</div>
						 <div id="hotel1" style="display:block" ;>
							
							{{ form_row(nuites.nuite1.hotel,{'attr':{'class':'paramHotel'}}) }}
							{{form_row(nuites.nuite1.categorieChambre)}}
						</div>
						<div id="hotel2" style="display:block" ;>
							<hr>
							{{ form_row(nuites.nuite2.hotel,{'attr':{'class':'paramHotel'}}) }}
							{{form_row(nuites.nuite2.categorieChambre)}}
						</div>
						
						 <div id="lblRestauration" style="display:block" ;>
					
							<h5>Réservation restaurations accompagnant</h5>
						</div>
						<div id="idRestauration1" style="display:block">
							<div class="from-control">
								{{form_row(nuites.inscription.restaurations)}}
							</div>
						</div> 			
	
					</div>
					<hr>
				<button class="btn btn-outline-primary" type="submit">Valider l'inscription</button>
				{{form_end(nuites)}}
				</div>
			</div>
		{% else %}
			<div class="alert alert-danger">Votre compte n'a pas le role nécessaire pour accéder à cette page.</div>
		{% endif %}

	{% endif %}


{% endblock %}
